import { createSlice } from "@reduxjs/toolkit";

export const habitsTrackrSlice = createSlice({
  name: "habitsTrackr",
  initialState: {},
  reducers: {},
});

export const {} = habitsTrackrSlice.actions;

export default habitsTrackrSlice.reducer;
